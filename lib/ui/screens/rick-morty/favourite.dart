import 'package:flutter/material.dart';

class SavedItemListScreen extends StatelessWidget {
  const SavedItemListScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
